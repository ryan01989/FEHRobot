float x, y; //for touch screen

    //Initialize the screen
    // LCD.Clear(BLACK);
    // LCD.SetFontColor(WHITE);

    // RCS.InitializeTouchMenu("0150F7IJN");
    // int lever = RCS.GetLever();
    // LCD.WriteLine(RCS.Time());
    // LCD.Clear();
    // LCD.WriteLine("Touch to Start");
    // LCD.Clear();
    LCD.WriteLine(cdsCell.Value());
    while(cdsCell.Value() > 0.3){}
    drive(2, 'b');
    drive(2, 'f');
    turn(100, 1);
    Sleep(1.0);
    drive(8, 'f');
    turn(191, 1);
    drive(1.5, 'f');
    turn(196, 0);
    Sleep(0.5);
    drive(25.7, 'f');
    Sleep(0.5);
    turn(191, 0);
    Sleep(0.5);
    // drive(10.5, 'f');
    while(cdsCell.Value() > 1.0 && (left_encoder.Counts() + right_encoder.Counts()) / 2. < (40.5*21)){
        left_motor.SetPercent(-1 * LEFT_MOTOR_CORRECTION_FACTOR * MOTOR_PERCENTAGE);
        right_motor.SetPercent(1 * RIGHT_MOTOR_CORRECTION_FACTOR * MOTOR_PERCENTAGE);
    }
    left_motor.Stop();
    right_motor.Stop();
    pressHumidifier();
    Sleep(2.0);
    drive(15, 'b');
    Sleep(2.0);
    turn(191, 1);
    Sleep(2.0);
    drive(38, 'b');
    Sleep(1.0);
    turn(100, 0);
    right_motor.SetPercent(-50 * RIGHT_MOTOR_CORRECTION_FACTOR);
    left_motor.SetPercent(50 * LEFT_MOTOR_CORRECTION_FACTOR);
    while((left_encoder.Counts() + right_encoder.Counts()) / 2. < (40.5*2) ){}
    left_motor.Stop();
    right_motor.Stop();
    Sleep(1.0);
    LCD.Clear();
    while(true){
        LCD.WriteLine("Done");
        Sleep(0.5);
    }
    
    // 0.26 for red, 0.4-0.5 for blue